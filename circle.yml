compile:
  pre:
    - autoreconf -i
  override:
    - ./configure
    - make -j 2
    - false
test:
  pre:
    - mkdir -p "$CIRCLE_TEST_REPORTS/junit"
  override:
    - make check GTEST_OUTPUT="xml:$CIRCLE_TEST_REPORTS/junit/"
